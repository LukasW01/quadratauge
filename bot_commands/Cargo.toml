[package]
name = "bot_commands"
version = "0.5.0"
edition = "2021"
description = "Library with a collection of some commands for the bot discord bot"
license = "MIT"
repository = "https://github.com/jontze/bot-rs"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
log = { workspace = true }
serenity = { workspace = true }
songbird = { workspace = true }
tokio = { workspace = true }
reqwest = { workspace = true }
serde = { workspace = true }
rand = "0.8.5"

[dependencies.bot_core]
path = "../bot_core"
version = "0.5.0"

[dependencies.bot_codegen]
path = "../bot_codegen"
version = "0.5.0"

[dependencies.bot_yt_playlist]
path = "../bot_yt_playlist"
version = "0.5.0"
